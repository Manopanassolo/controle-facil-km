'use client';

import { PrototypeFormDialog, PrototypeFormValues } from './PrototypeFormDialog';
import { DocumentKind, DocumentScope, useSessionActivity } from './SessionActivityProvider';
function text(values:PrototypeFormValues,key:string){const current=values[key];return Array.isArray(current)?current.join(' · '):current||'';}function formatDate(date:string){if(!date)return'Sem validade';const[y,m,d]=date.split('-');return`${d}/${m}/${y}`;}function warningDays(value:string){return Number(value.replace(/\D/g,''))||30;}function scopeValue(value:string):DocumentScope{return value==='Condutor'?'condutor':value==='Veículo'?'veiculo':'geral';}
export function DocumentsSessionModule(){const{documents,documentAlerts,addDocument,renewDocument,clearSessionDocuments}=useSessionActivity();const sessionCount=documents.filter((doc)=>doc.source==='session').length;function createDocument(values:PrototypeFormValues){addDocument({kind:text(values,'tipo') as DocumentKind,scope:scopeValue(text(values,'escopo')),subject:text(values,'vinculo'),expiryDate:text(values,'validade'),warningDays:warningDays(text(values,'alerta')),note:text(values,'observacao')});}function renew(documentId:string,values:PrototypeFormValues){renewDocument(documentId,text(values,'validade'),warningDays(text(values,'alerta')),text(values,'observacao'));}return <section className="panel"><div className="panel-title-row"><div><h2>Documentos monitorados</h2><span>Validade, alertas e bloqueios operacionais específicos.</span></div><div className="row-actions">{sessionCount?<button type="button" className="secondary-button" onClick={clearSessionDocuments}>Limpar documentos da sessão</button>:null}<PrototypeFormDialog trigger="+ Adicionar documento" title="Adicionar documento" description="Cadastre a validade. CNH, CRLV e Seguro vencidos podem bloquear apenas a operação que depende deles." onValidate={createDocument} fields={[{name:'tipo',label:'Tipo de documento',type:'select',required:true,options:['CNH','CRLV','Seguro','Comprovante','Contrato','Outro']},{name:'escopo',label:'Escopo',type:'select',required:true,options:['Condutor','Veículo','Geral']},{name:'vinculo',label:'Vinculado a',required:true,placeholder:'Nome exato do condutor ou veículo'},{name:'validade',label:'Data de validade',type:'date',required:true},{name:'alerta',label:'Alertar com antecedência',type:'select',required:true,options:['7 dias','15 dias','30 dias','45 dias','60 dias']},{name:'observacao',label:'Observação',type:'textarea',placeholder:'Informações adicionais'}]}/></div></div>{sessionCount?<div className="session-banner"><strong>{sessionCount} documento(s) criado(s) nesta sessão</strong><span>Nenhum arquivo foi enviado; a homologação valida regras de validade e operação.</span></div>:null}<div className="document-list">{documents.map((doc)=>{const alert=documentAlerts.find((item)=>item.documentId===doc.id);const status=alert?.state==='vencido'?'Vencido':alert?.state==='proximo'?'Próximo do vencimento':'Válido';return <div className={`document-row ${doc.source==='session'?'session-row':''}`} key={doc.id}><div className="document-copy"><strong>{doc.kind} · {doc.subject}</strong><span>Validade {formatDate(doc.expiryDate)} · alerta {doc.warningDays} dias · {doc.scope}</span>{alert?.blocksOperation?<span className="tag warning">Bloqueia operação correspondente</span>:null}{doc.source==='session'?<em className="session-chip">Somente nesta sessão</em>:null}</div><div className="row-actions"><span className={`tag ${status==='Válido'?'success':'warning'}`}>{status}</span><PrototypeFormDialog trigger="Renovar" className="secondary-button" title={`Renovar ${doc.kind}`} description="Atualize a validade; o bloqueio é recalculado imediatamente." onValidate={(values)=>renew(doc.id,values)} fields={[{name:'validade',label:'Nova validade',type:'date',required:true},{name:'alerta',label:'Alertar com antecedência',type:'select',required:true,options:['7 dias','15 dias','30 dias','45 dias','60 dias']},{name:'observacao',label:'Observação',type:'textarea',placeholder:'Número, protocolo ou observação da renovação'}]}/></div></div>;})}</div><div className="soft-box"><strong>Regra operacional</strong><span>CNH vencida bloqueia o condutor; CRLV ou Seguro vencidos bloqueiam somente o veículo vinculado. Documentos gerais não interrompem rotas automaticamente.</span></div></section>;}
