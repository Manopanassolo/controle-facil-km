import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const src=path.join(root,'src');const forbidden=[/mvNavigationV\d+/i,/mvWebLayoutV\d+/i,/mvShellV\d+/i,/v16\d{3,}\.js/i,/postprocess\.js/i,/querySelector(All)?\s*\(/i,/getElementById\s*\(/i,/\.style\.display\s*=/i,/\.style\.visibility\s*=/i,/addEventListener\s*\(\s*['"]click['"]/i,/setTimeout\s*\(/i,/setInterval\s*\(/i];const requiredModules=['dashboard','pendencias','agenda','notificacoes','campo','roteiros','historico','custos','veiculos','equipe','documentos','sinistros','relatorios','perfil','configuracoes'];function walk(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap((entry)=>{const full=path.join(dir,entry.name);return entry.isDirectory()?walk(full):[full];});}const files=walk(src).filter((file)=>/\.(?:ts|tsx|js|jsx|css)$/.test(file));const violations=[];for(const file of files){const text=fs.readFileSync(file,'utf8');if(!file.endsWith('.css'))for(const pattern of forbidden)if(pattern.test(text))violations.push(`${path.relative(root,file)}: forbidden legacy/runtime pattern ${pattern}`);if(/from\s+['"]\.\.\/\.\.\/\.\.\//.test(text))violations.push(`${path.relative(root,file)}: import outside V2 boundary`);}const shellFiles=files.filter((file)=>fs.readFileSync(file,'utf8').includes('function AppShell'));if(shellFiles.length!==1)violations.push(`Expected exactly one AppShell, found ${shellFiles.length}`);const modulesText=fs.readFileSync(path.join(src,'lib','modules.ts'),'utf8');for(const slug of requiredModules){if(!modulesText.includes(`slug: '${slug}'`))violations.push(`Missing canonical module route: ${slug}`);const page=path.join(src,'app',slug,'page.tsx');if(!fs.existsSync(page)){violations.push(`${slug} must have an independent physical route`);continue;}if(slug!=='dashboard'&&!fs.readFileSync(page,'utf8').includes('ModuleHeader'))violations.push(`${slug} must use the canonical ModuleHeader`);}const requirements=[['app/dashboard/page.tsx','DashboardByRole','Dashboard route must use role-aware dashboard'],['app/agenda/page.tsx','AgendaSessionModule','Agenda must use shared session workflow'],['app/pendencias/page.tsx','PendingSessionModule','Pending route must use live compliance workflow'],['app/roteiros/page.tsx','RouteSessionModule','Routes must use local KM homologation'],['app/campo/page.tsx','FieldJourneyModule','Field Mode must use guided homologation'],['app/historico/page.tsx','HistorySessionModule','History must consume shared session activity'],['app/custos/page.tsx','CostSessionModule','Costs must use shared fleet session module'],['app/veiculos/page.tsx','VehicleSessionModule','Vehicles must use shared fleet module'],['app/documentos/page.tsx','DocumentsSessionModule','Documents must use shared compliance module'],['app/sinistros/page.tsx','IncidentsSessionModule','Incidents must use shared incident workflow'],['app/layout.tsx','SessionActivityProvider','Root layout must provide shared session state'],['app/layout.tsx','DriverSessionProvider','Root layout must provide shared driver state'],['app/layout.tsx','IncidentSessionProvider','Root layout must provide shared incident state']];for(const[relative,needle,message]of requirements){const file=path.join(src,relative);if(!fs.readFileSync(file,'utf8').includes(needle))violations.push(message);}const providerText=fs.readFileSync(path.join(src,'components','SessionActivityProvider.tsx'),'utf8');for(const required of ['journeys','routes','expenses','appointments','vehicles','vehicleOptions','maintenanceAlerts','maintenancePendingCount','maintenanceRecords','scheduleMaintenance','completeMaintenance','documents','documentAlerts','documentPendingCount','blockedVehicleDocuments','blockedDrivers','addDocument','renewDocument','addVehicle','getVehicleKm','updateVehicleKm','nextMaintenanceKm','nextMaintenanceDate','totalKm','totalExpenses','completedAppointments','activityCount'])if(!providerText.includes(required))violations.push(`Session activity provider must expose ${required}`);if(!providerText.includes('Math.max(vehicle.currentKm,km)'))violations.push('Shared vehicle KM must never regress');if(!providerText.includes("alert.state!=='vencida'"))violations.push('Overdue maintenance must block vehicle');if(!providerText.includes("document.kind==='CNH'||document.kind==='CRLV'||document.kind==='Seguro'"))violations.push('Critical expired documents must define scoped operational blocking');if(!providerText.includes('!blockedVehicleDocuments.includes(vehicle.name)'))violations.push('Vehicle document blocking must affect only matching operational vehicle');const driverText=fs.readFileSync(path.join(src,'components','DriverSessionProvider.tsx'),'utf8');for(const required of ['blockedDrivers','driverOptions','addDriver','isDriverEligible'])if(!driverText.includes(required))violations.push(`Driver session provider must expose ${required}`);if(!driverText.includes("driver.status==='Ativo'&&!blockedDrivers.includes(driver.name)"))violations.push('Expired CNH must remove only matching active driver from eligibility');const incidentProviderText=fs.readFileSync(path.join(src,'components','IncidentSessionProvider.tsx'),'utf8');for(const required of ['vehicleOptions','driverOptions','pendingIncidents','addIncident','startIncidentReview','resolveIncident'])if(!incidentProviderText.includes(required))violations.push(`Incident provider must expose ${required}`);if(!incidentProviderText.includes('!vehicleOptions.includes(incident.vehicle)||!driverOptions.includes(incident.driver)'))violations.push('Incidents must reject ineligible vehicle/driver combinations');const incidentText=fs.readFileSync(path.join(src,'components','IncidentsSessionModule.tsx'),'utf8');for(const required of ['vehicleOptions','driverOptions','addIncident','startIncidentReview','resolveIncident','Exige ação posterior?'])if(!incidentText.includes(required))violations.push(`Incidents must use ${required}`);const pendingText=fs.readFileSync(path.join(src,'components','PendingSessionModule.tsx'),'utf8');for(const required of ['maintenanceAlerts','documentAlerts','documentPendingCount','pendingIncidents','Tratar ocorrência'])if(!pendingText.includes(required))violations.push(`Pending center must expose ${required}`);const historyText=fs.readFileSync(path.join(src,'components','HistorySessionModule.tsx'),'utf8');if(!historyText.includes('incidents')||!historyText.includes("type:'Sinistro'"))violations.push('History must include incident lifecycle');if(fs.existsSync(path.join(src,'app','[module]','page.tsx')))violations.push('Dynamic [module] page is forbidden');if(violations.length){console.error('Movvant V2 architecture guard failed:\n'+violations.join('\n'));process.exit(1);}console.log(`Movvant V2 architecture guard passed: ${files.length} source files, one AppShell, ${requiredModules.length} canonical modules, with shared incident, document, vehicle and driver eligibility enforced.`);
