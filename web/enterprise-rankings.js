import{createClient}from'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
const sb=createClient('https://aaqbwdrqmhuzqdesywze.supabase.co','sb_publishable_FDSQxTSJYyBdfRuV8ZwmGw_RwzO9-_Z',{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true,storage:window.localStorage}});
const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),money=v=>'R$ '+Number(v||0).toLocaleString('pt-BR',{maximumFractionDigits:0});
let companyId=null;
async function company(){if(companyId)return companyId;const s=(await sb.auth.getSession()).data.session;if(!s)return null;const a=await sb.from('user_role_assignments').select('company_id').eq('user_id',s.user.id).eq('active',true).limit(1).maybeSingle();companyId=a.data?.company_id||null;return companyId}
function renderList(id,rows,kind){const el=$(id);if(!el)return;if(!rows?.length){el.innerHTML='<div class="small">Sem dados dimensionais para o período selecionado.</div>';return}el.innerHTML=rows.map((r,i)=>`<div class="rank-row"><div><b>${i+1}. ${esc(r.dimension_name||r.dimension_key)}</b><div class="small">${kind==='seller'?'Loja '+esc(r.branch_code||'—'):`${r.active_customers||0} clientes · ${r.product_mix||0} produtos`}</div></div><div style="text-align:right"><b>${money(r.revenue)}</b><div class="small">Margem ${Number(r.margin_pct||0).toFixed(1)}% · ${r.orders||0} pedidos</div></div></div>`).join('')}
async function load(){const cid=await company();if(!cid)return;const from=$('from')?.value||'2026-08-01',to=$('to')?.value||'2026-08-31',branch=$('branch')?.value||'';let q=sb.from('commercial_dimension_monthly').select('period_month,dimension_type,dimension_key,dimension_name,branch_code,revenue,orders,active_customers,product_mix,margin_pct').eq('company_id',cid).gte('period_month',from.slice(0,7)+'-01').lte('period_month',to.slice(0,7)+'-01');if(branch)q=q.eq('branch_code',branch);const res=await q;const rows=res.data||[],branches=rows.filter(x=>x.dimension_type==='branch').sort((a,b)=>Number(b.revenue)-Number(a.revenue)).slice(0,12),sellers=rows.filter(x=>x.dimension_type==='seller').sort((a,b)=>Number(b.revenue)-Number(a.revenue)).slice(0,20);renderList('branchRanking',branches,'branch');renderList('sellerRanking',sellers,'seller')}
['branch','from','to'].forEach(id=>document.addEventListener('change',e=>{if(e.target?.id===id)load()}));
window.addEventListener('load',()=>setTimeout(load,400));
