{
  "name": "controle-facil-km",
  "private": true,
  "version": "164.0.1-clean-navigation",
  "scripts": {
    "build": "node build.js && node validate-core-flow.js && node validate-navigation.js && node validate-build.js"
  }
}
